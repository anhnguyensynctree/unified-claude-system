# OMS — personal/learned data only (framework is public)
agents/*/lessons.md
agents/*/MEMORY.md
agents/*/facts.json
agents/training/results.tsv
agents/shared-context/engineering/
agents/shared-context/product/
skills/compact-agent-memory.md

# Personal session data — never publish
sessions/
handoffs/
debug/
telemetry/
backups/
cache/
file-history/
tasks/
todos/
shell-snapshots/

# Project-specific memory (contains personal notes and session context)
projects/

# Learned skills (generated per-user, not part of base setup)
skills/learned/

# Temp files
*.tmp
*.log
*.bak
__pycache__/
.version-cache

# Plans (generated per-session)
plans/

# Local tool data
history.jsonl
paste-cache/
ide/
plugins/

# Facts extracted by mem0 (personal, per-project — lives in projects/ which is already excluded)
**/facts.json

# Local cache and settings
*-cache.json
settings.local.json

# Auto-memory and image cache
memory/
image-cache/
